// import Vue from 'vue'
import Vuex from 'vuex';

import regextest from './modules/regextest';

// console.log(Vue);
// Vue.use(Vuex);

export default new Vuex.Store({
    modules: {
        regextest
    }
})